# Base URL of the backend API. Adjust to match your environment.
VITE_API_BASE_URL=http://localhost:3000

# Socket.IO server origin for the simulation chat. Defaults to VITE_API_BASE_URL
# when unset; override if the gateway runs on a different host/port.
VITE_WS_URL=http://localhost:3000
